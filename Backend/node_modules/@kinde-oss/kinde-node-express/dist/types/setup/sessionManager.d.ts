import type { Express } from 'express';
/**
 * Attaches the `express-session` middleware and the `SessionManager` for internal
 * typescript SDK (in middleware form).
 * @param {Express} app
 */
export declare const setupKindeSession: (app: Express) => void;
